# Golden XLSX Fixtures

These files are provider-neutral Open XML inputs authored by the offline `GoldenFixtures` generator. Runtime contract tests verify the required manifest metadata and hashes and never regenerate these inputs through a Provider. Update a fixture only with a reviewed `UpdateReason` and a regenerated SHA-256 entry.
