# ClosedXML Provider Baseline

- Base commit: `94bb52e84ffc70634067b04541857433ef7af9df`。
- 执行时 worktree 已 dirty；本任务没有 reset、restore、clean、commit 或 push。
- Runtime: Windows `win-x64`，SDK `.NET 10.0.401`。
- Provider TFM: `net6.0;net8.0`；Abstractions/Core 继续为 `netstandard2.0`。
- ClosedXML 依赖锁定 `0.105.1`，依赖恢复需要可访问 NuGet。
- 任务状态脚本无法写入只读 `.agents/runtime`，执行状态由 `execution.md` 维护。
