# BO-RC-20260908-002 最终收口报告

- Task-ID：`BO-RC-20260908-002`
- HEAD：`e4093f554e7e81d642ff6722a1f8e44d185d39da`
- 环境：Windows 10 x64，.NET SDK 10.0.400，net6.0 runtime 6.0.36，net8.0 runtime 8.0.30。
- 审批上下文：`approvedBy=jian玄冰`；服务器预算 `2C4G`；生产实际并行度目标 `1`。
- 本报告只记录当前工作树和已生成证据，不执行 `git add`、`commit`、`push` 或 PR。

## 0. 本次计划收口（2026-09-14）

本次“.gitignore 与 API 快照单测修复计划”已完成本地实现和验证。`ai_docs/tasks/**/artifacts/**` 与所有 `packages.lock.json` 现在均被忽略；旧生成 artifact 保留在磁盘但不进入版本控制，成员级审批记录内容已迁移至 `ai_docs/tasks/BO-RC-20260908-002/api-breaking-approval.md`。

公共 props、Benchmark、ApiSnapshot、ResourceProbe 和双 TFM consumer 已移除 lockfile 配置，CI 改为普通 restore。API candidate/diff 改写入 CI 临时目录并作为 Actions artifact 上传，不再由 baseline 或 Git tracked-file 门禁绑定。

API generator 已升级为 `2.3.0`。baseline 继续保留批准人 `jian玄冰`、批准时间和完整公开成员集合，仅绑定源清单、4 个 Release 程序集、3 个 nupkg 及任务根审批文件的规范化文本 SHA-256；Core 批准哈希 `D664...` 未被 `BED899...` 替换。身份 self-test 已覆盖 LF/CRLF clean checkout、源/程序集/nupkg/审批文件篡改与忽略文件隔离。

本轮普通 restore、Release build、API identity self-test、双 TFM API compare、Unit、Integration、Docs 及双 TFM package consumer 均已通过；Unit 为各 `729/0/0`，Integration 各 `38/0/0`，Docs 为 `10/0/0`。本轮实施状态为 `COMPLETED`；历史 Review 中的生产 `2C4G`/并行度 `1` 与远端 CI 外部门禁仍按原记录保留，未伪造为本地验证结果。

## 1. 本轮目标

执行已批准计划的 P0-P9：锁定 restore/build/pack，补真实 Async File Integration 和模板边界，验证双 TFM PackageReference-only consumer，建立 API 治理和 breaking candidate 证据，修正 ResourceProbe 的真实请求并发模型，完成 CSV/UniqueTracker/cache/staging 性能证据、真实 IO Benchmark、文档治理和最终门禁。

## 2. 修改摘要

- 生产路径：CSV writer 生命周期复用和直接字段写入；UniqueTracker pending 集合复用；mapping cache key 复用静态 `JsonSerializerOptions`；修正条件格式扩展文件名。
- 测试路径：新增 CSV/Excel Async File Integration、模板异步边界、UniqueTracker、cache key 和 CSV 字节级职责测试。
- 工程路径：忽略并不提交 `packages.lock.json`，新增双 TFM package-only consumer，CI 使用普通 restore 并上传临时 API/package 证据，ResourceProbe 记录真实请求、队列指标和四入口 staging 结果。
- API 路径：按 `FIX-003` 成员级批准移除四个重复 `ExportToFile*` 扩展声明，保留对应接口实例成员，并同步迁移调用方与文档。
- 文档和证据：更新 README、Async 合同、AGENTS 规则，生成测试、Benchmark、资源矩阵、API governance 和 baseline 报告。

## 3. net6/net8

- `dotnet restore Bing.Offices.sln -m:1 --verbosity minimal`：通过。
- `dotnet build Bing.Offices.sln -c Release --no-restore /m:1`：通过，0 error；当前环境因 nuget.org 漏洞审计源不可达产生 `NU1900` 警告，不影响编译结果。
- 生产包资产：Abstractions/Core 为 `netstandard2.0`；Npoi 同时包含 `net6.0` 和 `net8.0` DLL/XML。
- API snapshot 双 TFM：各 `168` exported types、`1017` members，包含四个已批准删除的公共扩展，baseline compare 零差异。

## 4. Async Integration

新增真实文件路径 CSV/Excel async integration，覆盖 UTF-8、中文、逗号、引号、换行、CRLF/LF、替换、取消、异常原样传播、文件释放和清理；CSV 还覆盖大单字段、宽记录、异步写取消/异常、禁止同步 `Write`/`Flush` 及已写入首条记录后的取消。Integration 双 TFM 各 `38/0/0`，Round 7 TRX 记录于 `artifacts/test-results/integration-net6-round7/` 和 `artifacts/test-results/integration-net8-round7/`。

## 5. Template Async

当前 NPOI template 入口保留同步 `Read` 边界；async-only stream 的失败行为作为 characterization contract 固化，没有借本轮引入新的 template staging。覆盖取消、预取消、`leaveOpen` 和释放语义；双 TFM Template 测试均通过。文档已同步说明 DOM/template 仍是同步和高分配边界。

## 6. NuGet Consumer

新增 net6/net8 独立 PackageReference-only consumer，仅通过 `PackageReference` 引用 `Bing.Offices.Npoi` `2.0.0`，无 ProjectReference。使用任务包源和全新独立缓存执行普通 restore、Release build、run，双 TFM 均输出 `package-consumer-ok`；`project.assets.json` 显示 Npoi 为 package 节点。net6 仅保留 SDK EOL 的 `NETSDK1138` 警告；默认 nuget.org 不可达的失败日志没有被当作通过证据。

## 7. API Governance

API snapshot generator `2.3.0` 与 baseline 比较通过；四个重复 `ExportToFile*` 扩展已按 `FIX-003` 成员级批准删除，接口实例成员保持不变。snapshot generator 已排除编译器生成的 `AsyncStateMachineAttribute` 实现细节，语义 breaking diff 由 CI 在临时目录生成并上传，不写入 baseline。候选身份以 `e4093f554e7e81d642ff6722a1f8e44d185d39da` 作为捕获基线锚点，并由稳定的候选源文件清单 SHA-256、4 个程序集、3 个 nupkg 和任务根审批文件实际绑定；源清单不依赖存储 baseline 的提交 SHA。`identity-self-test` 已覆盖提交前 dirty、提交后 LF/CRLF clean checkout 及源/程序集/nupkg/审批文件篡改失败。治理分类覆盖 User API、Provider User API、Provider SPI、Compatibility 和 Execution detail；IVT 仍只在测试程序集之间存在。批准记录为 `ai_docs/tasks/BO-RC-20260908-002/api-breaking-approval.md`。本轮生产包 SHA256 已按当前 Release pack 重新记录在 candidate identity 中。

## 8. Resource Matrix

正式候选产物为 `artifacts/resource-formal-10000.jsonl`，共 `3 strategies × 3 scenarios × 4 concurrency = 36` cells，全部通过。请求档位 `1/4/16/64` 的 submitted/completed 分别为 `18/18`、`72/72`、`288/288`、`1152/1152`；全量 `maxActiveOperations=1`，64 档最大队列为 `63`，错误数和残留文件均为 `0`。峰值 WorkingSet `281,837,568` bytes，临时磁盘峰值 `4,176,231` bytes。该证据是 10,000 rows 受控候选，不宣称 100,000 rows 的生产预算证明。

最终入口 before/candidate 证据分别为 `artifacts/resource-entrypoints-before-clean-head.jsonl` 与 `artifacts/resource-entrypoints-fix007-fix008-n-a-final.jsonl`：两侧均覆盖 1K/10K/100K × `Export`、`ExportAsync`、`ExportToFile`、`ExportToFileAsync` × 3 次重复，共各 `36/36` 通过；两侧文件入口句柄 reopen 各为 `18/18`（适用 `18` 个场景），内存流入口各明确为 `18` 个 `N/A`，NPOI parser reopen、sampling 和 cleanup 均各为 `36/36`，操作结束无残留。`artifacts/resource-entrypoint-failure-probe-before-clean-head.jsonl` 与 `artifacts/resource-entrypoint-failure-probe-fix008-final.jsonl` 的 Setup/Sampler/Parser 受控失败各为预期 `3/3`，均记录异常并 cleanup；before source manifest 记录 clean-head 身份。

## 9. CSV 性能

CSV writer 改为一次创建、逐行复用并在末尾 flush，移除逐行 LINQ iterator。BenchmarkDotNet Dry/InProcess 单样本方向性结果：1M rows sync 从 `1,166.859 ms / 14,455.7 MB` 到 `203,723.9 us / 802,197.45 KB`；async 从 `1,316.551 ms / 14,455.7 MB` 到 `211,973.8 us / 802,197.57 KB`。字节级测试和双 TFM Unit 通过；未将 Dry 结果宣称为统计学结论。

## 10. UniqueTracker 性能

pending HashSet 和 first-row 字典通过栈复用，最大数量检查仍在创建状态前执行。100K×5 columns 方向性结果从 `178.986 ms / 261.36 MB` 到 `149.667 ms / 85.13 MB`；100K×1 和 10K×5 也有分配下降。重复、回滚、first-row、上限和空值标志职责测试通过。

## 11. Mapping Cache 性能

`ExcelMappingPlanCacheKey` 复用静态 `JsonSerializerOptions`，序列化字节保持等价，并覆盖 tenant/model/direction/version/config 隔离。单次 Dry 结果为 baseline `275.1 us / 22.05 KB`、优化后 `286.2 us / 21.90 KB`，未观察到稳定时间收益，因此未继续实施 fingerprint 重写。

新增 `CacheKeyCreation`、`PlanCacheHit`、`PlanCacheMiss` 三个 BDN 场景并保存 3 次迭代 raw；hit 使用语义相等的不同文档实例，miss 使用递增 tenant/configuration，场景内均有引用/语义校验。formal before/candidate mapping 各执行 `11/11`，结果见 `formal-before-mapping-3x` 和 `formal-candidate-mapping-3x`。

最新 `mapping-plan-miss-inprocess-fix006` 在每次 iteration 先预热不同 key，再以 `OperationsPerInvoke=4` 测量四个新身份并断言不复用 warm/previous plan；raw 的每个 workload 显示 `4 op`。

## 12. Async Staging

Excel async 文件路径使用可注入的 staging factory；每个测试使用独立临时目录，成功、异常和取消后均验证清理。完整入口矩阵使用 NPOI parser 实际重开 `Data` sheet 首尾数据行，并分别记录文件句柄、parser、采样和 cleanup 状态；正式 10K Resource Matrix 仍记录 P95/P99。ResourceProbe 的本地受控 gate 是共享 `SemaphoreSlim(1,1)`；目标生产入口的实际 gate 尚未提供，不能把本地证据当作生产配置，也不能据此推导 fully async 或 zero GC。

## 13. Benchmark

新增 `RealIoBenchmarks`，源码参数覆盖真实同目录文件的 CSV/Excel、sync/async、1K/10K/100K rows；before/candidate 各执行 `12/12`，当前 candidate raw 位于 `fix015-candidate-realio-3x/`。当前 candidate 100K mean：CSV sync `23.109 ms`、CSV async `35.049 ms`、Excel sync `1,474.354 ms`、Excel async `1,363.337 ms`；allocated 和 Gen0 详见 `artifacts/reports/formal-performance-comparison.md`。

controlled stream before/candidate 各执行 `12/12`，覆盖 delayed/throttled async 和 1K/10K/100K；当前 candidate 100K CSV delayed `1,039.539 ms`、CSV throttled `1,028.580 ms`、Excel delayed `1,485.935 ms`、Excel throttled `1,711.211 ms`，结果见 `fix015-candidate-controlled-3x/`。两侧 `realio-probe` 各 `24/24` 子进程场景通过，记录 mean/median/P95/P99/throughput/allocation/GC/PeakWS；三样本 percentile 只作可追溯测量，不作强 SLO 声明。

本轮新增候选子进程证据仍保留：`tail-latency-candidate-10000.jsonl` 对 `1/4/16/64` 并发各采集 `50,000` 个样本；`resource-probe-candidate.jsonl` 的 `16/16` 个资源场景通过。正式 `realio-probe-before/candidate-3x.jsonl` 已补齐 before/candidate；所有 BDN 和 probe 记录仍明确标记 `UNAPPROVED`。默认源 `NU1301` 失败日志保留，但显式本地 SDK 包源下 formal BDN 已实际执行成功。

## 14. GC/Memory

CSV 和 UniqueTracker 的分配方向性下降已由 BDN 记录；Resource Matrix 同时记录 WorkingSet、临时磁盘、allocated bytes、P95/P99 和 leftover files。NPOI workbook/template 仍属于 DOM 和高分配边界，未添加不受证据支持的 zero-GC 或 fully async 声明。长生命周期大 key 集合的 retained capacity 仍需目标环境持续观察。

## 15. Breaking Changes

四个重复 `ExportToFile`/`ExportToFileAsync` extension（Excel/CSV 各同步和异步一项）已按成员级批准删除。替代入口是对应同签名接口实例方法；双 TFM baseline 已更新并保持零 diff，扩展覆盖从 85 更新为 Core `15/15`、NPOI `66/66`、总计 `81/81`。编译器 async 状态机编号重排的快照属性行变化已随批准 candidate 固化。

## 16. Unit Test

Unit 双 TFM 均为 `729 passed / 0 failed / 0 skipped`，Round 7 TRX 为 `artifacts/test-results/unit-net6-round7/unit-net6-round7.trx` 和 `artifacts/test-results/unit-net8-round7/unit-net8-round7.trx`；包含 UniqueTracker 超限失败前状态回归、CSV BOM/异步写边界和 `81/81` 扩展覆盖门禁。Docs 测试为 `10 passed / 0 failed / 0 skipped`，未新增测试框架或覆盖率包。

## 17. Integration Test

现有双 TFM Integration 均为 `38 passed / 0 failed / 0 skipped`，Round 7 TRX 为 `artifacts/test-results/integration-net6-round7/integration-net6-round7.trx` 和 `artifacts/test-results/integration-net8-round7/integration-net8-round7.trx`；真实 CSV/Excel 文件测试、取消、异常、替换、异步写边界和释放路径均包含在内。独立 package consumer 使用当前重打 `2.0.0` 包，在任务包源下双 TFM 普通 restore/build/run 通过并输出 `package-consumer-ok`。formal BDN 使用显式本地 SDK 包源完成 before/candidate 对照；默认 `nuget.org` SSL 失败日志保留在基准证据目录。

## 18. Remaining Issues

- Resource Matrix 当前为 10,000 rows 受控正式候选，尚未完成计划中 100,000 rows 规模的生产证明。
- CI YAML 已增加双 TFM consumer、locked restore 和包检查；最新可查询的 GitHub Actions run `34223506781`（HEAD `e4093f554e7e81d642ff6722a1f8e44d185d39da`）在 Restore 阶段失败，Build/API/Test/Pack 均未执行，因此仍没有当前候选的最终绿灯证据。
- net6 仍产生 SDK EOL 生命周期警告。
- formal before/candidate 已完成 1K/10K/100K Real IO、controlled stream 和 mapping 隔离进程重复采样；当前 host 不是目标 2C4G 生产机，且 BDN 报告 processor 为 Unknown，不能直接形成生产预算批准。
- `artifacts/reports/staging-entrypoint-report.md` 已记录 clean-head before 与 candidate 四入口矩阵各 `36/36` 通过、文件入口句柄各 `18/18`、内存流句柄检查各 `18` 个 `N/A` 及两侧三种受控失败 cleanup；两侧使用同一 staging harness，生产源身份由 manifest 区分。

## 19. Release Blockers

1. 生产部署仓库需要确认 2C4G 入口的实际 semaphore/gate 确为并行度 `1`，本地 ResourceProbe 不能替代部署配置证明。
2. CI 需要由外部 GitHub 运行确认双 TFM、包 consumer 和发布门禁最终绿色。
3. formal before/candidate 性能与四入口 staging 对照已完成；仍需目标 2C4G 生产入口的资源预算/semaphore=1 证据，才能关闭发布性能门禁。

## 20. Release Recommendation

本次“.gitignore 与 API 快照单测修复计划”状态标记为 `COMPLETED`：代码、测试、包消费、API 实际身份校验与零差异均已完成。整体发布状态仍为 `PARTIAL`，因为目标 2C4G/单槽位生产证据和 GitHub CI 最终绿灯属于外部门禁，不能由本地结果替代。独立审查文件为 `ai_docs/tasks/BO-RC-20260908-002/review.md`，其 `NEEDS_FIX` 结论未由执行阶段改写；formal 对照索引见 `artifacts/reports/formal-performance-comparison.md`。

## 21. Review Fix Round 7

- `FIX-015`：已完成。修正 async CSV BOM 在空输出、已有前缀和非零位置场景的写入时序；CSV 专项双 TFM 各 `13/0/0`，全量 Integration 各 `38/0/0`。
- `FIX-014`：已完成。API candidate 现在校验稳定候选源文件清单、4 个程序集、3 个 nupkg 和任务根审批文件的规范化文本 SHA-256；generator `2.3.0`，baseline compare 双 TFM 零 diff，dirty/clean 提交契约和篡改 assembly/nupkg/source/approval 的负向校验均按非零退出验证，生成 candidate/semantic diff 不再作为 baseline 身份输入。
- `FIX-016`：已完成。当前候选已重新执行 Real IO `12/12`、controlled `12/12`、child probe `24/24`，并更新 formal 性能报告与候选源清单。
- `FIX-003`：`PARTIAL / BLOCKED_EXTERNAL_GATE`。仓库内性能、资源和 CI contract 证据已更新，但目标生产 2C4G 入口实际并行度 `1` 与当前候选 GitHub 双 TFM green run 仍需外部证据。
