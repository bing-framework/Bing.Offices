 开源 Provider 资源与运行时报告（F06）

## 容器基线

- 镜像固定到 `sha256:78235e09001f52b6592c458ac010775ebac6725422e80cd0c1650590f67b2743`；实际 SDK `8.0.425`，.NET 6 Runtime `6.0.36`（SHA512 校验）。
- 字体：`fonts-dejavu-core`、`fonts-noto-cjk`；`fc-match` 可解析 `Noto Sans CJK SC` 和 `DejaVu Sans`。
- 运行用户：UID/GID `10001`；源码挂载只读，工作区使用 `tmpfs`，构建在非 root 下完成。
- Docker Desktop Linux Engine：当时主机查询观察值 `29.4.0`，未保存于本轮历史原始产物；不将其视为可由旧文件独立复核的环境字段。后续验证脚本会保存版本和镜像身份 JSON。

## 已验证资源语义

- SpreadCheetah 只保留有界批次，串行等待消费者，不建立后台预取队列。
- SpreadCheetah 文件输出使用公共原子提交器；取消和写入失败不替换旧目标，Stream 目标保持调用方所有权。
- 本轮不验收 Aspose 页面图片或许可证路径。
- 容器构建过程中无字体缺失；此前无字体 slim 镜像的 NPOI 字体异常由固定字体包消除。

## 已测容量（不是批准阈值）

最终冻结代码证据：`artifacts/open-provider-20260926/docker-final/streaming-capacity.jsonl`、`streaming-capacity.log`。每单元独立进程与临时目录，容器内存限制 4 GiB，串行运行；预期为 EXPECTED_SUCCESS。样本为探针生成的平面数据，没有图片、模板或全量 DOM。运行时间为单次观察值，不是中位数/回归阈值；主机同时有正确性测试运行，不作性能排名或前后提升结论。

| 行数 | 批次 | 耗时 ms | 峰值工作集 bytes | 文件 bytes |
| --- | --- | --- | --- | --- |
| 100,000 | 100 | 1,230.39 | 74,407,936 | 1,188,671 |
| 100,000 | 1,000 | 1,197.03 | 75,579,392 | 1,188,671 |
| 100,000 | 10,000 | 1,104.22 | 78,327,808 | 1,188,671 |
| 500,000 | 100 | 2,643.97 | 76,668,928 | 6,064,663 |
| 500,000 | 1,000 | 3,000.20 | 76,820,480 | 6,064,664 |
| 500,000 | 10,000 | 2,459.01 | 79,847,424 | 6,064,664 |
| 1,000,000 | 100 | 3,671.08 | 76,648,448 | 12,160,740 |
| 1,000,000 | 1,000 | 4,129.56 | 77,848,576 | 12,160,740 |
| 1,000,000 | 10,000 | 4,049.80 | 81,268,736 | 12,160,740 |

9/9 EXPECTED_SUCCESS。最高观测工作集约 77.5 MiB。脚本配置为逐个独立进程串行运行，没有建立后台请求队列；这属于代码配置说明，原始 JSON 未测量并发度或队列长度。文件原子目标同目录暂存；成功后输出文件大小可见，样本未记录暂存空间峰值，因此不将文件大小称为峰值磁盘占用。

THRESHOLD_STATUS=BLOCKED_APPROVAL：没有批准性能阈值，本轮只报告实测，不称“百万行性能达标”。测量覆盖最终行循环和映射异常边界；无图片平面样本不能代表图片暂存、复杂报表或 DOM 路径性能。

## 容器合同证据状态

`docker-final/docker-smoke.log`：SheetContent 83、NPOI Report 25、ClosedXML Report 11、SpreadCheetah 57，每个 TFM 合计 176；net6.0/net8.0 共 352 项全部通过。

`docker-final/fontless-npoi-result.json`：无字体镜像包含 0 个字体文件；真实 NPOI AutoFit 返回 `BingOfficesExportException`，元数据为 `ExportFailed/NPOI/Export/Write`。同镜像 SpreadCheetah 输出 2,821 字节并成功完整读回中文，验证流式路径不依赖系统字体。

根目录旧 `f06-summary.json`、旧容量和旧全量日志保留为历史，标记 STALE_SOURCE_SNAPSHOT，不混入最终验收。CI 工作流配置成功不等于远程 CI 已执行。
