# Excel 07：错误与安全

导入结果提供 `IsSuccess`、成功/失败/总行数以及按 Sheet 的错误集合。错误包含 Sheet、行、列、Key、Header 和 RawValue 等上下文。

导入不执行公式，不访问外部链接，不把用户输入拼接为 SQL 或命令。文件流按调用方所有权处理；导出在 Workbook 完成前写入内部缓冲，失败或取消不会产生部分输出。动态列未知值可设置为失败，避免静默丢数据。
